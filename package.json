{
  "name": "book-management-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"next dev -p 3000\" \"npm --prefix backend run server\"",
    "build": "next build",
    "start": "next start",
     "api": "json-server --watch db.json --host 0.0.0.0 --port 10000",
    "server": "npm --prefix backend run server"
  },
  "dependencies": {
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4"
  },
  "description": "This repository is a Next.js frontend that talks to a JSON Server API for storing books.",
  "main": "next.config.js",
  "directories": {
    "lib": "lib"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
