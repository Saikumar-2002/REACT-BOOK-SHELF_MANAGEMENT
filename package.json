{
  "name": "book-management-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"next dev -p 3000\" \"json-server --watch db.json --port 3001\"",
    "build": "next build",
    "start": "next start",
    "server": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4"
  }
}
